---

to see the changes - git status

to check wheter the remote is added to current folder - git remote -v

to add the folder to remote repostiry :

1. git init
2. git remote add orgin ssh-link
3. git remote -v to check
3.


---
